# ChatTrace 0.3.0 (M3)

微信 4.x（4.1.11+）聊天数据工具链第三阶段：**聊天媒体（图片 / 语音 / 视频）消息级导出**。

## 新增能力（相对 0.2.0）
- **图片 dat 解密**：旧式单字节 XOR 图片（本账号实测 key `0xA4`，JPEG/PNG 自动按魔数识别）
  离线直解，消息 → 磁盘文件经 `packed_info_data` 内嵌 md5 精确关联
- **浏览页渲染**：可解图片直接在聊天时间线显示（懒加载）；V2 加密图与已清理文件用
  ⚠️ 如实标注，不再是无意义的占位符
- **语音导出**：解密库内 `VoiceInfo.voice_data`（SILK v3）按会话消息精确关联，导出原始 `.silk`（保真留档）
- **视频导出**：明文 mp4 / 缩略图拷贝；原视频被微信清理时自动降级为缩略图并注明
- **HTML 导出含媒体**：勾选"包含媒体"后，HTML 附写 `<会话>_assets/` 目录并在页面内嵌
  图片 / 语音 / 视频链接；txt / json 导出附媒体可用性状态
- **CLI**：`chattrace export --media`（新增参数）
- 测试 16 → 29（新增 dat 解密向量 / md5 提取 / 媒体定位与语音库关联用例）

## 已知限制
- **V2 加密图片**（文件头 `07 08 56 32 08 07`，约 2025-08 之后新收图片，与本账号密钥无
  离线派生关系）需微信运行内存中的专用 AES key，**离线不可解**，界面标注提示
- 语音导出为微信原始 `.silk` 格式（不内置转码器；转码需自行引入 SILK 解码器）
- 文件类消息（49）正文 XML 为加密格式，消息级关联未实现；`msg/file` 目录为明文可自行备份

## 使用
- Web UI：浏览聊天 → 消息图片直接显示；导出页勾选"包含媒体"导出 HTML
- CLI：`chattrace export --account-dir <xwechat_files\wxid_xxx> --user <好友> --format html --media`

## 系统要求
- Windows 10/11，微信 4.1.11+，已用 0.2.0+ 流程完成密钥获取与解密

## 许可与合规
- MIT License · Copyright (c) 2026 Qiao Xinliang (qiaodogbear)
- 仅用于导出本人账号、本机聊天数据；非官方工具，与腾讯/微信无关联；无云端/网络上报
- 媒体解密算法为本项目独立推导实现（XOR 反推自文件头 + 图像魔数，真实数据验证），
  第三方依赖与参考来源见仓库内 `NOTICE.md` / `REFERENCES.md`
