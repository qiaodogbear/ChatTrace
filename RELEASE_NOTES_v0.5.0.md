# ChatTrace 0.5.0 (M5)

**V2 加密图片攻破**：2025-08 之后微信新收的图片（V2 容器）现在可以**完全离线解码**——
不需要微信在运行、不需要读进程内存、不需要任何在线服务。这是 4.x 图片导出最难的一道门槛。

## 核心突破：V2 图片密钥可离线派生

### 曾经的结论是错的
0.3.0/0.4.0 的笔记里写着"V2 密钥只存在于运行进程内存，离线不可解"。这次做完了最后一轮攻坚，
结论被推翻：**密钥一直在本机磁盘上，只是需要正确的派生路径。**

### V2 容器布局
```
[15B 头][AES-128-ECB 密文][单字节 XOR 尾部]

头 := 07 08 "V2" 08 07 | u32le aes_size | u32le xor_size | 1B 占位
```
最容易算错的一点：AES 段按 PKCS7 补齐到 16 的整数倍，而 `aes_size` 本身已是 16 的倍数时
**仍会再多占一个块** —— 真实密文长度是 `aes_size + 16 - (aes_size % 16)`（本账号 `1024 → 1040`）。
把多出来的 16 字节误当作"明文段"，分段就会整体错位。

### 两把钥匙怎么来的
| 量 | 来源 | 本账号实测 |
| --- | --- | --- |
| `code` | `%APPDATA%\Tencent\xwechat\...\kvcomm\key_<code>_*.statistic` 文件名 | `1234567` |
| `xor_key` | `code & 0xFF` | `0xA4` |
| `aes_key` | `md5(f"{code}{wxid}").hexdigest()[:16]`（16 字节 ASCII） | 已校验 |

`wxid` 取账号目录名去掉尾部数字后缀。**候选密钥必须通过校验才会启用**：用它在 AES-ECB 下解密
该账号自己的 V2 图片首块，明文应为图片魔术（`FF D8 FF E0/E1`、`89 50 4E 47`…）。

### 走过的弯路（留档，避免后人重复）
- 直接拿消息 XML 里的 per-image `aeskey` 试解本地 dat —— 无效（那是 CDN 传输用的）。
- 假定 key 是数据库主密钥的派生 —— 全部变体试解失败。
- 假定 key 以 32 字符明文常驻进程内存 —— 对运行中的微信做全内存搜索
  （1.4 GB 可读写区 + 1.1 GB 全量区、ASCII 与 UTF-16 两种编码、16 字节对齐与逐字节暴力，
  累计 15 亿+ 次 AES 尝试）**全部落空**。原因正是 key 不常驻，而是每次由 `code` 现算。

## 新能力

- **V2 图片解码**：新增 `service/image_key.py`，负责在 `kvcomm` 缓存中枚举候选 `code`、派生密钥、
  并用本账号真实图片校验后缓存；`media.py` 的 `decode_dat()` 随之支持 V2 容器。
- **自动降级**：少数原图是微信私有的 **WxAM（`wxgf`）** 压缩格式（实测约占原图的 14%），
  这类消息会自动改用同一张图的**缩略图**呈现 —— 用户照样看得见这张图，卡片上会注明原图格式。
  解码路径按"原图 → 缩略图"依次尝试，任一成功即显示。
- **诊断可见**：`/api/state` 的 `capabilities.v2_images` 返回密钥状态（命中情况与来源路径），
  只给指纹、不输出明文密钥；界面不再出现"新版加密格式，离线无法解密"的旧文案。

## 顺带修正：卡片尺寸/大小现在描述"你正在看的那张图"

V2 图片开始显示后，一个旧问题变得刺眼：卡片角标一直显示消息 XML 里的尺寸，
而 XML 记录的是**发送方上传的那一档**，不是本机这一档。实测同一个 md5 下微信会存三档：

| 文件 | 本机实测 | XML 字段 |
| --- | --- | --- |
| `<md5>_h.dat`（高清原图） | 4096×3072 · 8.8 MB | — |
| `<md5>.dat`（中图） | 210×157 · 268 KB | `width`/`height`/`length` |
| `<md5>_t.dat`（缩略图） | 157×210 · 4 KB | `cdnthumb*` |

所以角标写着 210×157，实际显示的却是一张 4096×3072 的原图。现在改为**测量选中文件本身**：
按"原图优先"选择候选后，解密其头部并解析真实像素尺寸（JPEG 走 SOF、PNG 走 IHDR、GIF 走逻辑屏幕描述符），
大小也用该文件的实际字节数。多段 Exif/ICC 会把 SOF 推到 64 KB 之后，因此探测窗口分两档自动加宽。

另外补了一次**闭环校验**：三个样本解密后的明文 MD5 与消息 XML 记录的 `<img md5>` **完全一致**
（`bd3cf890…`、`5b11e459…`、`83e922b0…`），证明"消息 → 文件"的关联与解密结果都正确，而不是只看格式头对不对。

## 测试
56 → **106** 个用例。新增 `tests/test_image_key.py`（32 个）：wxid 归一化、密钥派生
（含真实账号的 `code`/`key` 回归向量）、候选校验（正确 key 命中 / 错误 key 拒绝）、
V2 容器往返解码（含 `aes_size` 非 16 倍数）、`kvcomm` 目录扫描、
解析器在"无 code / 无模板 / 有诱饵 code"下的行为与缓存失效。
另加 `tests/test_media_dimensions.py`（18 个）：JPEG/PNG/GIF 尺寸解析（含远端 SOF 与多段 App）、
dat 分类与 XOR 密钥反推回归、V2 明文中段拼接（防 16 字节错位）、原图优先与缩略图回退。
另有防泄漏断言：诊断输出中不得出现明文密钥。

## 真机验证（微信 4.1.13.65，真实账号）
- 磁盘采样 **313 个 V2 图片文件**：**270 个直接解码为完整 JPEG/PNG**
  （逐个校验 SOI/EOI 与 IEND 结构完整），43 个为 WxAM 原图（自动回退到缩略图），**0 个失败**。
- 密钥解析耗时 **0.23 s**，逐文件头部探测 **0.66 ms**。
- Web UI 端到端：群聊图片消息从"说明卡片"变为**直接显示**，
  `/api/media/file` 返回校验通过的 JPEG（`ff d8 ff e0 … ff d9`）。
- 既有能力回归：25 个加密库解密、106 个用例、语音播放、231 条消息导出（22 图片卡片 + 18 播放器 + 22 JPG + 18 WAV）全部通过。
- 会话页实测：60 个会话共 **395 条图片消息全部可直接显示**（0 条降级为说明卡片）；
  抽查解码结果含 4096×3072（8.8 MB）、4608×3456（3.8 MB）、1256×5528（长截图）等原图。

## 依赖变化
无新增依赖（AES 由既有 `pycryptodomex` 提供）。

## 已知限制
- **WxAM（`wxgf`）原图**：仍需专用 HEVC 解码器，当前回退到缩略图。
- **文件本体**：`msg\file\` 中文件的消息级精确关联尚未实现（该目录内为明文原文件，可整目录备份）。
- **表情**：本机未缓存表情文件，显示为统一表情卡片。
- **平台**：目前仅 Windows x64。

## 升级说明
覆盖安装即可。旧版本解出的内容缓存（`media_cache`）可继续使用；V2 图片会在下次浏览时自动解码。
