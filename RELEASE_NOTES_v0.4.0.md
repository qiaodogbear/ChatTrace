# ChatTrace 0.4.0 (M4)

消息解析层重构：修掉"人名错位、消息乱码、媒体解析不出、语音只能下载文件"四类问题。

## 修复与改进

### 1. 发言人解析（修复人名与内容错位）
- **根因**：`Name2Id` 的 rowid 是**分片内局部**的——同一个 rowid 在 `message_1.db` 与
  `message_2.db` 里指向不同的人。旧实现把各分片的映射合并成一张表，导致人名与消息内容错位。
- **现在**：按消息所属分片解析；并优先采用群聊消息自带的 `<发送者wxid>:` 前缀。
  真机验证：200 条群聊消息中**未识别发送者从大量降为 0**。

### 2. 乱码与错位（修复文本渲染）
- **根因**：绝大多数富媒体消息的 `message_content` 是 **Zstandard 压缩**的二进制
  （`WCDB_CT_message_content = 4`），被当作字符串读取自然满屏乱码。
- **现在**：解压后再解析。解压得到完整 XML，图片尺寸、语音时长、文件名、链接标题等
  全部可用；文本消息、系统消息也走同一条解码路径。

### 3. 媒体显示（替换空洞占位符）
- 统一"媒体卡片"，按类型渲染：图片（缩略图 + 尺寸/大小角标）、语音（播放条 + 时长）、
  视频（内嵌播放器 / 缩略图）、文件（文件名 + 类型）、链接（标题 + 地址）、
  引用（被引用原文 + 被引用人）、小程序、位置（地名 + 坐标）、语音通话（时长）、名片（昵称/微信号）。
- 无法离线解密的图片、已被微信清理的媒体，显示为**信息完整的说明卡片**（类型 + 尺寸/时长 + 原因），
  不再出现"⚠️ 一句话"或空白。

### 4. 语音在界面内直接播放
- 集成 **silk-python**（BSD-3-Clause）：把微信 SILK v3 语音本地解码为 WAV，聊天页与导出的 HTML
  里都是标准播放条，**不再提供"点击下载 .silk 文件"**。
- 解码结果按会话缓存，重复播放即时响应；解码不可用时回退为统一语音卡片（含时长），而不是文件链接。

## 依赖变化
- 新增 `zstandard>=0.22`（BSD-3-Clause）：解压压缩的消息体。
- 新增 `silk-python>=0.2.8`（BSD-3-Clause，提供 `pysilk`）：语音转码。
  （社区常用的 `pilk` 为 GPL-3.0，本项目未采用，以避免许可传染。）

## 测试
29 → **56** 个用例，新增：跨分片发送者解析、Zstandard 消息体解压、各类型 XML 元数据提取
（图片/语音/视频/位置/通话/文件/引用/表情/名片）、SILK→WAV 转码往返与缓存。

## 真机验证（微信 4.1.12.55，48 万条消息）
- 群聊发送者未识别数：0；富媒体卡片按类型正确渲染；
- 18 条语音全部转码为 WAV，浏览器 `canplaythrough` 验证可播放；
- 单会话 231 条消息导出 HTML：22 张图片卡片 + 18 个内嵌播放器 + 22 个 JPG + 18 个 WAV。

## 已知限制
- **V2 加密图片**（2025-08 后新收部分图片）密钥只在微信运行内存中，**离线不可解**，
  显示为带尺寸与原因的卡片；后续计划通过运行时密钥提取解决。
- 表情本机未缓存文件，显示为统一表情卡片；文件本体（`msg\file\`）的消息级关联未实现。

## 许可与合规
- MIT License · Copyright (c) 2026 Qiao Xinliang (qiaodogbear)
- 仅用于导出本人账号、本机聊天数据；非官方工具，与腾讯/微信无关联；无云端/网络上报
- 第三方依赖与许可见 `NOTICE.md`；格式研究来源见 `REFERENCES.md` 与 `docs/media-format-notes.md`
